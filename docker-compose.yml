services:
  encuesta:
    build: .
    container_name: encuesta_gabinete_social
    ports:
      - "8080:80"
    environment:
      - SUPABASE_URL=${SUPABASE_URL:-}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY:-}
    restart: unless-stopped
